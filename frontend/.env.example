# Production: set to your deployed API origin, e.g. https://api.example.com. Leave empty only if the API is same-origin.
# Optional when frontend and API share an origin.
PUBLIC_API_BASE_URL=http://localhost:8000

# Enable Demo mode explicitly (defaults to enabled in DEV)
VITE_ENABLE_DEMO=false
